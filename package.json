{
  "name": "web",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "push-deploy" : "git push origin master && npm run build && firebase deploy",
    "deploy" : "npm run build && firebase deploy"
  },
  "dependencies": {
    "@astrojs/tailwind": "^4.0.0",
    "astro": "^2.7.0",
    "tailwindcss": "^3.3.2"
  }
}
